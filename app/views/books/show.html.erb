<h2>User info</h2>

<div class="profile-container">

  <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %>
  <h3 class="profile-name">name   <%= @user.name %></h3>
  <h3 class="profile-introduction">introduction   <%= @user.introduction %></h3>
  <% if @user.id == current_user.id %>
    <p><%= link_to "プロフィール編集", edit_user_path(@user), class: "pull-right" %></p>
  <% end %>
</div>

<%= form_with model:@book, local:true do |f| %>
  <h2>New Book</h2>
  <div class ="field">
  <label>Title</label>
  <%= f.text_field :title %>
  </div>

  <div class = "field">
  <label>Opinion</label>
  <%= f.text_area :body %>
  </div>

  <%= f.submit 'Create Book' %>

<% end %>

<h2>Book detail</h2>
<table>
  <thead>
    <tr>
    <th>Image</th>
    <th>Title</th>
    <th>Opinion</th>
    <th colspan="3"></th>
    </tr>
  </thead>
  <body>
  <tr>
    <td><%= attachment_image_tag @bookone.user, :profile_image, :fill, 100, 100, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg" %><br>
    <%= link_to @bookone.user.name, user_path(@bookone.user.id) %></td>
    <td><%= @bookone.title %></td>
    <td><%= @bookone.body %></td>
  </tr>
  </body>
</table>


<% if @bookone.user.id == current_user.id %>
<%= link_to "Edit", edit_book_path(@bookone.id) %></td>
<%= link_to 'Destroy', book_path(@bookone.id) ,method: :delete, data: { confirm: "Are you sure?" } %></td>
<% end %>